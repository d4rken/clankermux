{
  "$ref": "#/$defs/PublicRunwayDto",
  "$defs": {
    "PublicRunwayDto": {
      "type": "object",
      "properties": {
        "schema": {
          "type": "string",
          "const": "clankermux.public.runway.v1"
        },
        "intervalKind": {
          "$ref": "#/$defs/PublicFixedHorizonKind"
        },
        "generatedAt": {
          "type": "string",
          "description": "INSTANT this payload describes.",
          "format": "date-time"
        },
        "horizonMs": {
          "type": "number",
          "description": "DURATION, ms — the horizon the scan modelled, so no client hardcodes it.",
          "minimum": 0
        },
        "coverage": {
          "type": "object",
          "properties": {
            "activeKeyCount": {
              "type": "integer",
              "minimum": 0
            },
            "statedKeyCount": {
              "type": "integer",
              "minimum": 0
            },
            "unobservedKeyCount": {
              "type": "integer",
              "minimum": 0
            },
            "learningAccountCount": {
              "type": "integer",
              "description": "Accounts, deduped across active keys, whose burn is not measured yet — early in a window, at 0%, or on a window that has not started. A COUNT, never ids. Above zero with `worstStatedOutcome: null` means \"not yet\", which is a different thing from \"no evidence\".",
              "minimum": 0
            }
          },
          "required": [
            "activeKeyCount",
            "statedKeyCount",
            "unobservedKeyCount",
            "learningAccountCount"
          ],
          "additionalProperties": true
        },
        "worstStatedOutcome": {
          "anyOf": [
            {
              "$ref": "#/$defs/PublicWorstOutcomeDto"
            },
            {
              "type": "null"
            }
          ]
        }
      },
      "required": [
        "schema",
        "generatedAt",
        "horizonMs",
        "coverage",
        "worstStatedOutcome"
      ],
      "description": "`GET /public/v1/runway` — the POOL-LEVEL quota projection and nothing else.\n\nDeliberately absent, and none of it may be added back:\n\n - API key ids, key NAMES (they look like `\"impatience (claude)\"`), routing    pins and per-key eligible-account mappings. All of it is management data    on an unauthenticated surface, and the per-key breakdown is three array    levels past what the device's scanner can descend into.  - Per-account utilization, resets and observation times. That is the    accounts resource's job; re-serving it here would be the same measurement    in two places, and the two would drift. A cause REFERENCING an account id    is a resource reference, which is a different thing.",
      "additionalProperties": true
    },
    "PublicFixedHorizonKind": {
      "type": "string",
      "enum": [
        "fixed_horizon",
        "other"
      ],
      "description": "Starts at envelope generatedAt; ends at generatedAt + horizonMs."
    },
    "PublicWorstOutcomeDto": {
      "type": "object",
      "properties": {
        "kind": {
          "$ref": "#/$defs/PublicRunwayKind"
        },
        "exhaustsAt": {
          "type": [
            "string",
            "null"
          ],
          "description": "INSTANT the pool is projected to be out; null on every other kind.",
          "format": "date-time"
        },
        "causes": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/PublicRunwayCauseDto"
          }
        },
        "earliestExhaustsAt": {
          "type": [
            "string",
            "null"
          ],
          "description": "The quantisation band around  {@link  exhaustsAt } : the earliest and latest run-out the same scan reports when each whole-percent reading is nudged by half a percent either way.\n\nProviders report utilization as a whole percent, and the projection divides by that number, so the error is proportional to the runway itself — at 20% one day into a weekly window, half a percent is about six hours. A single instant states a precision the input never had.\n\nEither end is null when the scan at that perturbation found no run-out inside the horizon: the band is OPEN on that side, not zero-width. Both are null when no band is stated at all (modelled reset credits make the burn non-monotonic, or every reading was already fractional).",
          "format": "date-time"
        },
        "latestExhaustsAt": {
          "type": [
            "string",
            "null"
          ],
          "format": "date-time"
        },
        "headroomPct": {
          "type": [
            "number",
            "null"
          ],
          "description": "Signed pace headroom: how much more load the pool can take, or how much it has to shed, as a whole percentage of the currently measured pace.\n\nTHE figure a \"more agents or fewer agents\" reading is built from, and the only one on this surface that is pool-level rather than per-account. The per-class burn ratios on `/public/v1/pacing` answer a different question and routinely disagree with this one: several accounts can each be over pace on their own window while the pool, with staggered resets and failover, still has room.\n\nPositive with `direction: \"margin\"`, negative-sense with `\"deficit\"`. The sign is carried in `headroomDirection` rather than in the number so a client that ignores the enum cannot silently read a required cut as spare capacity.\n\nBOTH null is not \"zero headroom\" — it is \"no figure\", and which end of the scale that means depends on `kind`. On a `beyond_horizon` it is the good end (no probed increase up to the cap flips the verdict, so headroom is at least the cap); on a `runway` it is the bad end (no probed slowdown down to the floor clears the horizon). A renderer must read `kind` alongside it and must never draw either case as a zero.",
          "minimum": 0
        },
        "headroomDirection": {
          "anyOf": [
            {
              "$ref": "#/$defs/PublicHeadroomDirection"
            },
            {
              "type": "null"
            }
          ]
        }
      },
      "required": [
        "kind",
        "exhaustsAt",
        "causes",
        "earliestExhaustsAt",
        "latestExhaustsAt",
        "headroomPct",
        "headroomDirection"
      ],
      "description": "The pool's worst STATEABLE quota outcome.\n\n\"Stated\", not \"active\", and the distinction is the whole point: an UNOBSERVED key could be worse than every stated one, so a name implying this covers everything would be a claim the data does not support. `coverage` is what says how much of the pool the figure speaks for, and a client rendering this without it is claiming more than it knows.",
      "additionalProperties": true
    },
    "PublicRunwayKind": {
      "type": "string",
      "enum": [
        "runway",
        "out_now",
        "beyond_horizon",
        "unknown",
        "no_accounts",
        "other"
      ],
      "description": "Quota-runway outcome, as a CLOSED set.\n\nMirrors `RunwayOutcome[\"kind\"]` value for value, in this surface's snake_case rather than core's kebab-case, with `other` for a kind added later. Mapping a future kind onto `beyond_horizon` would be the worst possible default — it reads as \"nothing runs out\" — so the escape hatch is what keeps a new outcome from being silently reassuring.\n\nQUOTA, not availability: an account that is paused or cooling off still counts as capacity here, exactly as it does for `/api/runway`. Copy built on this must say \"quota\", never \"available\"."
    },
    "PublicRunwayCauseDto": {
      "type": "object",
      "properties": {
        "accountId": {
          "type": "string",
          "description": "A join key against `accounts[].id`, so never truncated."
        },
        "windowKind": {
          "$ref": "#/$defs/PublicWindowKind"
        }
      },
      "required": [
        "accountId",
        "windowKind"
      ],
      "description": "The account + window that runs out at the reported instant.",
      "additionalProperties": true
    },
    "PublicWindowKind": {
      "type": "string",
      "enum": [
        "five_hour",
        "seven_day",
        "weekly_scoped",
        "other"
      ],
      "description": "The quota windows this surface names."
    },
    "PublicHeadroomDirection": {
      "type": "string",
      "enum": [
        "margin",
        "deficit",
        "other"
      ],
      "description": "Which way the pool's pace headroom points, as a CLOSED set.\n\nTwo members and an escape hatch rather than a signed number alone, because the two mean opposite things and a client dropping the sign would invert the advice completely."
    }
  },
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "urn:clankermux:public.runway.v1",
  "$comment": "Generated from public DTOs. Unknown properties are accepted; producer allowlist tests enforce privacy. Compatibility metadata fields are optional for older v1 servers."
}
